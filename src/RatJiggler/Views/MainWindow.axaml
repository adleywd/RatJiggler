<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RatJiggler.Views"
        xmlns:vm="using:RatJiggler.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="RatJiggler.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/logo.ico"
        Title="Rat Jiggler"
        Height="700"
        Width="550"
        CanResize="False"
        ShowInTaskbar="True"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight"
        SystemDecorations="Full"
        UseLayoutRounding="True"
        RequestedThemeVariant="Dark">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <!-- Key Bindings for Keyboard Shortcuts -->
    <Window.KeyBindings>
        <!-- Ctrl + R to Start Movement -->
        <KeyBinding Gesture="Ctrl+R"
                    Command="{Binding StartMouseMovementByHotkeyCommand}" />

        <!-- Ctrl + C to Stop Movement -->
        <KeyBinding Gesture="Ctrl+C"
                    Command="{Binding StopMovementCommand}" />
    </Window.KeyBindings>

    <Grid Margin="15" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />    <!-- Tab Control -->
            <RowDefinition Height="Auto" /> <!-- Auto Start Checkbox -->
            <RowDefinition Height="Auto" /> <!-- Status Message -->
        </Grid.RowDefinitions>

        <!-- Tab Control for Modes -->
        <TabControl Grid.Row="0"
                    Margin="0,0,0,20"
                    BorderThickness="1"
                    SelectedIndex="{Binding SelectedTabIndex}">
            
            <!-- Normal Movement Tab -->
            <TabItem Header="Normal Movement"
                     Cursor="Hand">
                <local:NormalMovementView DataContext="{Binding NormalMovementViewModel}" />
            </TabItem>

            <!-- Realistic Movement Tab -->
            <TabItem Header="Realistic Movement"
                     Cursor="Hand">
                <local:RealisticMovementView DataContext="{Binding RealisticMovementViewModel}" />
            </TabItem>
        </TabControl>

        <!-- Auto Start Checkbox -->
        <CheckBox Grid.Row="1"
                  IsChecked="{Binding AutoStartMovement}"
                  Content="ⓘ Start movement automatically when application starts"
                  Margin="0,10,0,0"
                  HorizontalAlignment="Center"
                  ToolTip.Tip="When enabled, mouse movement will start automatically when the application starts"/>

        <!-- Status Message -->
        <TextBlock Text="{Binding StatusMessage}"
                   Grid.Row="2"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,0"
                   FontSize="14"
                   FontWeight="Bold"
                   Foreground="{Binding StatusMessageColor}" />
    </Grid>
</Window>